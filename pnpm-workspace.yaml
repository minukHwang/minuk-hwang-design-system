packages:
  # Layer packages follow packages/<layer>/<framework>.
  # Framework-agnostic packages (tokens, esbuild-config) sit one level up.
  - 'packages/*'
  - 'packages/*/*'
  - 'apps/*'

public-hoist-pattern:
  - '*vanilla-extract*'
  - '*esbuild*'
  - '@minuk-hwang-design-system/*'
