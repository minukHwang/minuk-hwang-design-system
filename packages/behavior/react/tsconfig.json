{
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    // Where declaration output lands
    "outDir": "./dist",
    // Source root, mirrored by the dist layout
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  // The barrel file is not an individual build target
  "exclude": ["src/index.ts"]
}
